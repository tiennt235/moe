# /moe ask "&lt;question&gt;"

The default command. Route the question to the matching expert(s), delegate, and return a
single synthesized answer with every source citation preserved (`source · section · page`).

Select more than one expert only when the question genuinely spans domains. If no expert
fits, say so rather than forcing one.
